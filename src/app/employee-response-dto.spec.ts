import { EmployeeResponseDto } from './employee-response-dto';

describe('EmployeeResponseDto', () => {
  it('should create an instance', () => {
    expect(new EmployeeResponseDto()).toBeTruthy();
  });
});
