import { EmployeeRequestDto } from './employee-request-dto';

describe('EmployeeRequestDto', () => {
  it('should create an instance', () => {
    expect(new EmployeeRequestDto()).toBeTruthy();
  });
});
